import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  background: {
    flex: 1,
    backgroundColor: '#0d1f16' // color base opcional para fallback
  }
})

export default styles
