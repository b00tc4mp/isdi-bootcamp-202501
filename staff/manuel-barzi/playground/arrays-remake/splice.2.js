Array.prototype.splice = function (indexFrom, removeCount, element) {
    /*
    TODO implementar los siguienes pasos
    crear array con elementos a retornar (si los hay, si no, array vacío)
    iterar el array desde indexFrom hasta indexFrom + removeCount
    copiar elementos iterados al array removedElements
    copiar element en las posiciones iteradas
    despues del iterador, retornar removedElements
    */

    const removedElements = []

}