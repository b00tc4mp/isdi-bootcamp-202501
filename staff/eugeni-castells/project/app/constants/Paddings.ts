const baseRem = 16;

export const spacing = {
  rem: baseRem, // 1rem = 16
  xs: baseRem * 0.25, // 4
  sm: baseRem * 0.5, // 8
  md: baseRem, // 16
  lg: baseRem * 1.5, // 24
  xl: baseRem * 2, // 32
  xxl: baseRem * 3, // 48
};

export const Paddings = {
  screenPadding: 2 * spacing.rem,
};
